# TaskBridge — подробный план доработок

Дата сверки: 11 сентября 2026 года.
Статус: план реализации. Пункты с [ ] — оставшиеся работы, а не выполненные изменения.

## 1. Цель

Основной сценарий: начать работу на ПК через TaskBridge, закрыть терминальный клиент, продолжить ту же сессию с телефона и вернуться на ПК с общей историей и состоянием.

TaskBridge владеет процессом агента, историей, рабочей копией и результатами. Терминал, локальный браузер и облачный интерфейс — клиенты. Отключение клиента не останавливает агента.

Вторая цель: довести работу до проверенного изменения в репозитории — просмотр diff, verification, ограниченные исправления, явное применение или сохранение результата.

Сначала этот сценарий реализуется на Pi. Другие runners, параллельность, reviewer и Android-клиент развиваются отдельными этапами. Сохраняются Node.js, Pi RPC, локальный SQLite, артефакты на диске и LAN. Vercel уже реализован; новый relay с нуля не проектируется.

## 2. Исходное состояние

- master: HEAD 8a023f7; package.json рабочей копии содержит версию 0.6.0.
- В рабочей копии есть незакоммиченные изменения, включая model-catalog, local-models, pi-settings, сервер, TaskManager, UI и тесты. Они учитываются как существующий код, но не как выпущенная версия.
- Облачная ветка: feature/cloud-transport, HEAD 2477b73. Общая база с master: da4122b.
- После общей базы в master отдельно добавлен cloud bridge на Vercel Queues.
- Документ docs/cloud-transport-status.md в облачной ветке датирован 9 сентября: локальные проверки описаны как пройденные, живой Vercel/Postgres-деплой не подтверждён. Это исторический отчёт.
- При подготовке плана прочитаны код, тесты и документация. Тесты, сервер и внешний деплой заново не запускались.
- Перед реализацией состояние веток сверить повторно: рабочая копия развивается параллельно.

### 2.1. Уже реализовано — повторно не разрабатывать

| Область | Состояние | Основание |
| --- | --- | --- |
| Хранилище | SQLite, миграция JSON/JSONL, seq событий, backup, ограничение хвостов текста | src/task-store.mjs, scripts/backup.mjs |
| Единственный сервер | Lock на каталог данных | src/instance-lock.mjs |
| Pi | RPC, сообщения, steer/follow-up, compact, STOP с завершением дерева процессов | src/pi-rpc.mjs, src/task-manager.mjs |
| История | Пагинация, SSE с Last-Event-ID, heartbeat, догрузка после курсора | src/server.mjs, web/app.js |
| Git | Worktree, новые и бинарные файлы в patch через временный index | src/git.mjs, tests/git.test.mjs |
| Apply | Применение patch в source checkout, dirty/HEAD и git apply --check; без автоматического коммита | src/task-manager.mjs, src/git.mjs |
| Очистка | Ручное удаление worktree, очистка при удалении задачи, поиск сирот при старте | src/task-manager.mjs |
| Verification | Команды, exit code, логи; провал переводит задачу в FAILED | src/git.mjs, src/task-manager.mjs |
| Загрузки | Потоковый multipart, staging, лимиты, TTL | src/multipart.mjs, src/uploads.mjs |
| Модели | Каталог Pi, model/thinking; local-models развивается в рабочей копии | src/model-catalog.mjs, src/local-models.mjs, src/pi-settings.mjs |
| AUTO | Базовый выбор локального профиля text/vision | src/dispatcher.mjs |
| Импорт | Поиск native session и продолжение исходного файла после подтверждения закрытия терминального Pi | src/native-sessions.mjs |
| Auth | Pairing, cookie, Origin-проверки; в примере выключен | src/auth.mjs, config.example.json |
| Уведомления | Notification API при работающей странице; не фоновый Web Push | web/app.js |
| Cloud в master | Vercel Queues, локальный outbox, replay, ограниченная дедупликация | src/cloud/*, cloud/*, docs/cloud-bridge.md |
| Cloud в ветке | Другая реализация API/PWA, Postgres, approvals, нормализация событий, метрики, деплой-скрипты | feature/cloud-transport:docs/cloud-transport-status.md |

### 2.2. Подтверждённые ограничения

1. TaskManager напрямую создаёт PiRpcSession и совмещает задачу, сессию, запуск, verification и runtime.
2. activeTaskId ограничивает выполнение одной inference-задачей. Новая задача при занятости может отклоняться, несмотря на наличие queue.
3. После restart активные задачи получают FAILED_RECOVERY. Историю можно продолжить; непрерывное исполнение после сбоя не обеспечено.
4. Нет taskbridge pi с attach/detach.
5. LAN API сообщений не имеет общего устойчивого commandId-контракта. Cloud-дедупликация не закрывает все аварийные окна.
6. Импорт использует исходный native session-файл; sidecar-lock обычный Pi не соблюдает.
7. Нет ограниченного repair loop. Patch собирается до verification и может устареть, если проверки меняют файлы.
8. Patch сравнивается с текущим HEAD workspace. Полноту результата после коммитов агента относительно baseCommit необходимо доработать.
9. Apply доступен для FAILED/CANCELLED: успешные проверки не обязательны для применения.
10. Очистка сирот существует; нужен аудит сохранности результатов, а не ещё один механизм удаления.
11. Cloud-реализации расходятся по хранилищу, API, конфигурации и Root Directory.
12. Видимые несовпадения cloud-ветки и рабочей копии: createTask передаёт id в input вместо options.requestedId; setModel вызывается с объектом вместо provider/modelId; setThinking отличается от setThinkingLevel.

## 3. Правила реализации

- SQLite остаётся источником локального состояния исполнения. Облако хранит транспортные данные выбранного варианта; отсутствие отдельной облачной БД не означает отсутствие облачного состояния.
- Каждый этап завершается пользовательским сценарием. Массовую перестановку каталогов не совмещать с изменением поведения.
- Сохранять taskId, пути артефактов и существующие seq при миграции.
- Disconnect/detach, abort/cancel, archive/delete имеют разные действия и семантику.
- STOP и ответы агенту не ждут завершения генерации в очереди управления.
- Не обещать exactly-once для внешнего Pi RPC без доказанной поддержки; неопределённый результат после сбоя показывается явно.
- PID — диагностическое поле, а не способ подключения к потерянному stdio.
- Неприменённый результат не удаляется только из-за возраста или терминального статуса задачи.
- AUTO выбора профиля сохраняется. Выбор runner/платного провайдера — отдельная функция с явной политикой пользователя.

## 4. Этап 0 — baseline и согласование cloud

Приоритет: P0. Зависимости: нет.
Результат: воспроизводимая база и выбранный путь развития Vercel-кода.

### Работы

- [ ] Зафиксировать коммиты, незакоммиченные изменения и версию Pi. Сохранить пользовательскую работу; не переключать грязную рабочую копию ради интеграции.
- [ ] Выполнить npm run check и npm test; отдельно записать исходные ошибки.
- [ ] Проверить cloud-ветку в отдельном checkout/worktree, сопоставить возможности с master.
- [ ] Подготовить решение: основной cloud-вариант, причины, переносимые возможности второй реализации и миграция настроек.
- [ ] Выбирать по подтверждённому деплою, требованиям пользователя, инфраструктуре, сохранности очередей и стоимости переноса. Размер реализации не является критерием качества.
- [ ] Согласовать направление до переноса данных/изменения действующего деплоя. Развилка не блокирует локальный Session/CLI MVP.
- [ ] Согласовать createTask, setModel, setThinkingLevel, поля модели и ошибки; добавить интеграционные проверки вызовов.
- [ ] Переиспользовать подходящие нормализатор событий, approvals и метрики cloud-ветки.
- [ ] Описать миграцию queued-команд/outbox: дренирование либо остановка приёма, сохранение commandId, учёт недоставленных событий и откат. Не включать два исполнителя одной команды.
- [ ] Обновить единый гайд с корректными Root Directory и переменными окружения.

### Приёмка

- [ ] START создаёт одну задачу с ожидаемым id; model/thinking работают через выбранный cloud API.
- [ ] LAN работает при выключенном облаке.
- [ ] Реальный Vercel: создание, follow-up, STOP, reconnect, restart локального сервера, недоступность облака. Без окружения пункт остаётся явно непроверенным.
- [ ] Документация различает «написано», «проверено локально» и «проверено на живом окружении».

Области: src/cloud, cloud, server/TaskManager, config.example.json, cloud-тесты и документация.

## 5. Этап 1 — доступ и восстановление клиента

Приоритет: P0. Зависимости: baseline; cloud-проверки используют решение этапа 0.
Результат: телефон возвращается из фона в актуальную задачу и показывает состояние сервера/агента.

- [ ] Включить auth по умолчанию в новых конфигурациях. Для существующего явного enabled:false сохранить совместимость с предупреждением; описать переход.
- [ ] Предусмотреть защищённый локальный доступ CLI без копирования cookie. Не распространять loopback-доверие на облачный proxy.
- [ ] При visibilitychange → visible и восстановлении сети обновлять выбранную задачу, состояние агента и события, а не только доступность ПК.
- [ ] Проверить snapshot/SSE/polling: старый ответ не откатывает новое состояние, события до подписки не теряются.
- [ ] При backlog больше maxEventsPerRequest догружать страницы до зафиксированной верхней границы, затем переходить к live.
- [ ] После очистки streaming-delta восстанавливать экран через завершённые сообщения/snapshot. Не считать каждый разрыв seq потерей данных.
- [ ] Разделить «нет связи», «idle», «генерация», «compact», «verification», «процесс завершился». HTTP heartbeat не доказывает работоспособность модели.
- [ ] Ограничить повторы при офлайне, исключить несколько подписок после reconnect.

Приёмка:

- [ ] Reload, блокировка экрана, авиарежим и смена сети не теряют завершённые сообщения и не дублируют вывод.
- [ ] Восстанавливается backlog больше страницы, в том числе после очистки промежуточных дельт.
- [ ] Два браузера видят согласованную историю; отключение браузера не останавливает Pi.
- [ ] Завершение процесса не показано как idle; непарный клиент не получает управляющий доступ при включённом auth.

Области: auth, server, web/app.js, web/chat-state.mjs, event-window/event-trim, cloud UI.

## 6. Этап 2 — Session, Run и минимальный Runner

Приоритет: P0. Зависимости: baseline и контракт событий; локальная реализация не ждёт деплоя.
Результат: управление Pi отделено от задач, существующий UI продолжает работать.

### Модель данных

| Сущность | Назначение | Минимальные поля |
| --- | --- | --- |
| Task | Цель, проверки, результат | id, projectId, sessionId, status, resultRevision |
| Session | Долгоживущий разговор и workspace | id, runnerId, nativeSessionRef, workspaceRef, state, revision |
| Run | Prompt, repair или retry | id, taskId, sessionId, kind, status, startedAt, finishedAt, verification |
| Command | Команда и результат доставки | id, sessionId, runId, clientId, type, payloadHash, status, result |

Для MVP одна Task связана с одной Session. Prompt в idle создаёт Run; steering относится к активному Run. Произвольное объединение задач в сессию откладывается: сохраняется пространство taskId/seq.

TaskBridge sessionId и native sessionId различаются. Перезапуск процесса сам по себе не создаёт новую пользовательскую сессию.

### Работы

- [ ] PiRunner — тонкая оболочка PiRpcSession: start/resume, send, abort, getState, события, close.
- [ ] Capabilities для steer, compact, model/thinking и запросов UI; будущие runners не обязаны поддерживать всё.
- [ ] SessionManager и AgentHost как внутренний владелец процессов; сначала в одном серверном процессе.
- [ ] TaskManager сохраняет задачи, workspace, verification, результаты; разговор переносится постепенно.
- [ ] sessions/runs/commands в существующей SQLite: версия схемы, транзакционная миграция, backup перед изменением.
- [ ] Session-связь для старых задач. Старые Run восстанавливать только по доступным фактам, не выдумывать точную историю.
- [ ] Сохранить native-файлы, taskId, артефакты, seq и старые HTTP-маршруты через совместимый слой.
- [ ] Определить владение workspace: удаление Task не уничтожает Session/каталог с другими ссылками или активным процессом.
- [ ] Переиспользовать нормализацию событий cloud-ветки; native-события сохранить для диагностики.

Приёмка:

- [ ] Старые задачи продолжаются после миграции; повтор миграции не создаёт дубликаты.
- [ ] Создание, follow-up, compact, STOP, model/thinking и Apply сохраняют поведение.
- [ ] TaskManager работает через Runner, Pi JSONL остаётся в адаптере.
- [ ] Проверены восстановление backup и отказ старой версии открывать неподдерживаемую схему.

Новые модули: src/session-manager.mjs, src/agent-host.mjs, src/runners/pi-runner.mjs и хранение sessions/runs поверх SQLite.

## 7. Этап 3 — единый приём команд и несколько клиентов

Приоритет: P0. Зависимости: этап 2; cloud-интеграция — решение этапа 0.
Результат: LAN, CLI и cloud управляют общей сессией по одинаковым правилам.

### Команды

- [ ] Ввести commandId, clientId, sessionId, тип, версию протокола и hash payload.
- [ ] Одинаковый commandId/payload возвращает сохранённый результат; тот же id с другим содержимым — конфликт без исполнения.
- [ ] Фиксировать принятие в SQLite до передачи runner. Различать ACCEPTED, DISPATCHING, COMPLETED, REJECTED, UNKNOWN_AFTER_CRASH. Принятие не означает завершённую генерацию.
- [ ] Последовательно менять состояние сессии, но не блокировать управление до окончания prompt. STOP и ответы на запросы проходят во время работы.
- [ ] Описать приоритет STOP/approval и отмену ещё не отправленных сообщений.
- [ ] После аварии сверять неизвестную доставку с runner/history, если возможно. Не повторять неидемпотентный prompt без доказательства, что он не принят.
- [ ] Срок хранения commandId согласовать с TTL транспорта. Не считать любое seq ниже максимального дубликатом: возможны доставка не по порядку и разные приоритеты.
- [ ] Для подтверждений сохранять requestId, срок действия и первый валидный ответ; поздний ответ другого клиента не повторяет действие.

### Attach и история

- [ ] Добавить список сессий, snapshot, историю и подписку; attach/detach не создают и не завершают Pi.
- [ ] Для MVP сохранить task-scoped seq и однозначное Session → Task; расширение курсора версионировать.
- [ ] Snapshot содержит revision и границу replay; события до перехода на live буферизуются.
- [ ] Старый/недоступный курсор вызывает явный resync из snapshot и сохранённой истории.
- [ ] Native branch/entry cursor добавлять после проверки установленной версии Pi. Наличие RPC-методов не предполагать по старому анализу.
- [ ] Attach и команды проверяют права независимо от наличия корректного sessionId.

Приёмка:

- [ ] Одновременные команды клиентов принимаются в определённом порядке; история согласована.
- [ ] Потерянный ACK и обычный retry commandId не создают второй prompt.
- [ ] Crash до отправки, после отправки и до фиксации ответа не вызывает скрытого повтора; неоднозначность видна пользователю.
- [ ] STOP проходит при занятом runner и накопленных сообщениях; повторный/просроченный approval не действует второй раз.

Области: command-sequencer, SQLite commands, SessionManager, API, cloud dispatcher, reducers.

## 8. Этап 4 — taskbridge pi и ПК → телефон → ПК

Приоритет: P0. Зависимости: этапы 2–3.
Результат: терминальный клиент для повседневного продолжения общей сессии.

### CLI и daemon

- [ ] Локальная bin-команда taskbridge; публикация npm-пакета не требуется для MVP.
- [ ] Команды: pi, pi --new, sessions, attach <id>, status, daemon start/stop/status и doctor.
- [ ] По cwd определять зарегистрированный проект; неизвестный путь не регистрировать молча.
- [ ] При одной подходящей сессии подключаться; при нескольких показывать название, проект, модель, время и статус с выбором стрелками/Enter. Для скриптов — явный id.
- [ ] При отсутствии сервера стартовать скрытый daemon, ждать readiness с таймаутом; при ошибке показывать диагностику.
- [ ] Адаптировать instance-lock: два одновременно запущенных CLI не создают два daemon; проверить гонки и Windows.
- [ ] CLI использует серверный API, не запускает собственный Pi.
- [ ] MVP показывает текст, tool-события, модель, ошибки и состояние. Полное воспроизведение Pi TUI не требуется.
- [ ] Ctrl+D и /detach отключают клиент; закрытие окна сохраняет daemon. Ctrl+C во время работы прерывает Run, сохраняя Session.
- [ ] Compact, модель/thinking и запросы ввода поддерживаются согласно capabilities.
- [ ] Doctor проверяет Node/Git/Pi, доступ к data/workspace, instance-lock, доступность host/runtime и совместимость протокола; не печатает секреты.

### Native sessions

- [ ] Переиспользовать существующий индекс/picker.
- [ ] Добавить импорт в TaskBridge-owned session-файл; проверить поддерживаемый способ копирования/ветвления и целостность истории.
- [ ] При изменении исходного файла требовать закрытие native writer либо получать проверенный стабильный снимок. Произвольное копирование во время записи не считать гарантией.
- [ ] Продолжение оригинала оставить явным режимом: TaskBridge не гарантирует, что обычный Pi не откроет его снова.
- [ ] Показывать фактический workspace: основной checkout или worktree. Копия истории не изолирует файлы проекта.

### Главный критерий готовности

1. Запустить taskbridge pi, отправить задачу, меняющую файл A.
2. Во время работы закрыть терминал: Pi продолжает выполнение.
3. Подключиться с телефона, увидеть историю и отправить изменение файла B.
4. Вернуться через taskbridge pi, увидеть оба запроса и продолжить работу.
5. Без сбоя процесса сохраняются тот же native sessionId и единственный процесс-владелец.
6. Повторить с одновременным CLI/PWA, обрывом сети и повтором команды.

До прохождения этого сценария не расширять проект несколькими новыми runners.

## 9. Этап 5 — отдельный AgentHost и recovery

Приоритет: P1. Зависимости: этапы 2–4.
Результат: restart HTTP/cloud gateway не прерывает генерацию; сбой host обрабатывается отдельно.

- [ ] Вынести AgentHost в фоновый процесс с защищённым локальным IPC. Он владеет Pi, SessionManager и очередями управления.
- [ ] Gateway становится клиентом host. Host — единственный исполнитель команд; gateway не меняет задачи напрямую и не конкурирует за старый server-lock.
- [ ] Runtime descriptor: instanceId host, IPC endpoint, session/run, native reference, workspace, PID, время запуска.
- [ ] Restart gateway подключается к живому host по IPC; stdio по одному PID не восстанавливается.
- [ ] После crash host выявлять осиротевшие процессы, проверять владельца перед освобождением ресурсов, согласовывать незавершённые команды.
- [ ] Различать «история восстановлена», «готово к продолжению», «выполнение возобновлено». Автоматический resume — для доказуемо безопасных случаев.
- [ ] Для прерванного Run/неизвестного исхода команды дать явное действие продолжения.
- [ ] Добавить опциональный Windows autostart в контексте пользователя без обязательной системной службы.

Приёмка:

- [ ] Restart gateway при генерации не меняет Pi PID и не теряет сообщения.
- [ ] Crash host не создаёт второго писателя в native session и не повторяет неоднозначную команду молча.
- [ ] После перезагрузки ПК доступны история, результаты и понятное продолжение.
- [ ] Graceful shutdown сохраняет принятые команды/outbox; ошибки IPC/startup видны через doctor.

## 10. Этап 6 — актуальный результат, review diff и repair

Приоритет: P1. Зависимости: этапы 2–3. Исправления patch и UI diff можно выполнять раньше отдельного host.
Результат: применяемое изменение соответствует реально проверенному состоянию файлов.

### 10.1. Результат и Apply

- [ ] Собирать финальный patch после verification либо повторно после изменяющих файлы проверок.
- [ ] Связать resultRevision, baseCommit, patch hash, verification и Run с одним снимком результата.
- [ ] Перед Apply выявлять устаревание после follow-up, внешнего изменения worktree и форматирования.
- [ ] Просмотр по файлам/hunks, статистика, binary/deleted/renamed, скачивание patch.
- [ ] Сохранить временный Git index; не заменять его git add -N в пользовательском индексе.
- [ ] Сохранить dirty/HEAD/git apply --check. Force не должен незаметно обходить отдельный verification gate.
- [ ] При настроенных проверках обычный Apply — для актуального успешного результата. FAILED/CANCELLED — отдельное явное действие с причиной и журналом.
- [ ] NOT_CONFIGURED показывать отдельно от PASSED: отсутствие проверок не означает успешные тесты.

### 10.2. Ветка, повторное применение и очистка

- [ ] Keep branch: явное сохранение результата в именованную ветку с коммитом, без автоматического push/merge.
- [ ] После коммитов агента diff включает изменения относительно сохранённого baseCommit, а не только текущего HEAD worktree.
- [ ] Повтор Apply возвращает уже применённый статус. Follow-up после Apply получает новую revision и определённую базу сравнения.
- [ ] Archive не уничтожает workspace; delete — отдельное действие.
- [ ] Проверить sweepOrphans: неизвестный каталог сначала инвентаризируется/показывается пользователю, а не признаётся заведомо ненужным.
- [ ] Автоочистка — явно включённая политика при доказанном сохранении результата. Неприменённые/несохранённые изменения сохраняются.
- [ ] Перед удалением закрыть использующий каталог процесс, проверить границы путей и отсутствие других владельцев Session.

### 10.3. Verification и ограниченный repair

- [ ] Сохранить совместимость с массивом verification-команд; расширенный формат добавляет имя шага, timeout и признак изменения файлов.
- [ ] Проверки берутся из доверенной конфигурации; агент не может убрать обязательный gate правкой проектного файла.
- [ ] После провала отправлять в ту же сессию ограниченный фрагмент лога, команду и exit code; полные логи сохранять.
- [ ] Ввести autoRepair/maxRepairAttempts: первоначально максимум два исправления при явно включённой функции.
- [ ] Каждый repair — отдельный Run со своей verification, длительностью и результатом.
- [ ] STOP отменяет процесс verification и будущие попытки, а не только меняет флаг.
- [ ] Ошибки инфраструктуры, auth и недоступной модели не направлять в бесконечное исправление кода.
- [ ] После лимита сохранить patch/evidence, показать причину остановки и ручное продолжение.

Приёмка:

- [ ] Форматтер меняет файл во время проверки: UI/Apply используют обновлённый patch.
- [ ] Коммит агента в worktree входит в результат относительно baseCommit.
- [ ] Тест падает, repair исправляет причину; отдельный сценарий доказывает предел попыток.
- [ ] STOP во время verification завершает дочерний процесс и не запускает repair.
- [ ] Обработаны устаревший результат, изменившийся source HEAD, повтор Apply, binary, новый файл.
- [ ] Неприменённые результаты переживают archive, restart и очистку.

Области: src/git.mjs, TaskManager/Run lifecycle, артефакты, API, diff UI и тематические тесты.

## 11. Этап 7 — постоянная очередь и ресурсы

Приоритет: P1. Зависимости: этапы 2–3 и определённое восстановление Run; параллельный repair — этап 6.
Результат: следующая задача принимается, даже когда текущая выполняется.

- [ ] Сначала сохраняемая очередь с capacity=1: добавление, отмена, порядок, причина ожидания, восстановление после restart.
- [ ] Заменить activeTaskId учётом ресурсов: local endpoint/GPU, remote provider/account, verification и workspace.
- [ ] Capacities настраиваются; одинаковую пропускную способность провайдеров не предполагать.
- [ ] Для одной Session/workspace разрешать одного изменяющего файлы исполнителя. Разные задачи в общем checkout также блокируют запись друг для друга.
- [ ] Согласовать загрузку/смену/выгрузку модели с занятостью ресурса: не обрывать чужой Run.
- [ ] Освобождать ресурсы после STOP, ошибки запуска, timeout, verification; зависший lock не блокирует очередь навсегда.
- [ ] WAITING_INPUT удерживает необходимые ресурсы, сохраняя защиту workspace от другого писателя.
- [ ] Таймауты ожидания/исполнения, ограниченный retry транспорта; retry не смешивается с repair кода.

Приёмка:

- [ ] Три задачи идут по порядку при capacity=1; вторую принимают во время первой.
- [ ] Локальная и удалённая модель через Pi работают параллельно на независимых ресурсах.
- [ ] Restart не запускает один Run дважды и не оставляет ресурс навечно занятым.
- [ ] Два писателя в workspace не допускаются.

Зависимости задач — отдельное расширение: одного dependsOn недостаточно, нужно определить, какой commit/patch получает зависимая задача.

## 12. Этап 8 — дополнительные runners

Приоритет: P2. Зависимости: стабильный этап 4 и Runner-контракт; параллельное выполнение — этап 7.
Результат: второй агент работает в общем жизненном цикле Task/Session/Run.

- [ ] Выбрать сначала один runner из Claude Code/Codex по реальному сценарию, подключать по одному.
- [ ] Проверить официальный интерфейс установленной версии: запуск, structured output, resume, abort, permissions, запросы ввода, Windows.
- [ ] Реализовать capabilities/нормализацию; UI скрывает недоступные compact/steer/model controls.
- [ ] Сохранить общие worktree, verification, артефакты, команды, recovery; не создавать независимые PiTask/ClaudeTask/CodexTask.
- [ ] Авторизация провайдера — штатный механизм; секреты не включаются в события/артефакты.
- [ ] Смена runner не обещает продолжение той же native session: это новый Run/native session с явной передачей задачи, workspace и разрешённого контекста.
- [ ] После проверки второго runner добавить третий тем же способом.

Приёмка: создание, streaming, запрос ввода, STOP, resume, verification и результат проходят общие контрактные сценарии каждого адаптера.

## 13. Последующий backlog

Эти работы не блокируют основной Session/CLI MVP.

| Возможность | Содержание | Критерий готовности |
| --- | --- | --- |
| Independent reviewer | Отдельный запуск по задаче, diff и проверкам; структурированные замечания; техническое ограничение записи | Не меняет workspace; число review/repair-раундов ограничено; доступно решение пользователя |
| Фоновые уведомления | Web Push или выбранный внешний канал; текущие уведомления страницы переиспользуются | Приходят при закрытом клиенте, без дублей, отключаются |
| Метрики моделей | Переиспользовать local-models и cloud-метрики; показывать источник | Нет выдуманных VRAM/температур; отсутствующие данные явно недоступны |
| Project profiles | Версионированные runner/verification-настройки, секреты отдельно | Недоверенный файл не отменяет проверки без принятия настроек |
| AUTO между runners | Детерминированная политика ресурсов, доступности, разрешённых провайдеров | Видна причина выбора; запрещённый облачный provider не используется |
| Task dependencies | Циклы, ошибки предшественников, передача commit/patch | Зависимая задача стартует на определённой базе и получает изменения |
| MCP | Адаптер существующих команд и прав | Не обходит auth, commandId, ограничения запуска |
| E2EE | Модель угроз, проверенный протокол, управление ключами | Проверены pairing, отзыв устройства, смена ключей; TLS не называется E2EE |
| KMP Android | После стабилизации протокола: share target, уведомления, подключение | Проходит общие attach/replay/command-сценарии без своей orchestration-логики |
| Установка/публикация | doctor, инструкция, решение владельца о лицензии, README, демонстрация | Новый пользователь воспроизводит сценарий; публикация выполняется отдельно |

## 14. Порядок небольших поставок

| Поставка | Состав | Проверяемый результат |
| --- | --- | --- |
| A | Baseline, расхождения cloud, решение об интеграции | Воспроизводимая база и единый план cloud |
| B | Auth defaults, возврат из фона, полная догрузка | Телефон возвращается в актуальную задачу |
| C | PiRunner без изменения поведения | Текущие сценарии работают через адаптер |
| D | Session/Run, SQLite-миграция | Старые задачи продолжаются |
| E | Command ledger, последовательность, attach API | Предсказуемые два клиента и retry |
| F | CLI, daemon start, detach/attach | ПК → телефон → ПК в LAN |
| G | Адаптация выбранного cloud, живой smoke | Та же сессия доступна через Vercel |
| H | Отдельный AgentHost, recovery | Restart gateway не прерывает агента |
| I | Актуальный patch, diff UI, Keep branch, очистка | Результат проверяется и сохраняется |
| J | Отменяемая verification, repair | Исправления в заданных пределах |
| K | Сохраняемая очередь, затем scheduler | Задачи ждут и выполняются по ресурсам |
| L | Второй runner, затем третий | Общий lifecycle для нового агента |

B и исправление актуальности patch из I можно делать независимо от крупного рефакторинга. H не нужен для первого LAN CLI MVP. G зависит от выбранного существующего cloud, а не от всех будущих функций. Номера релизов назначаются после оценки объёма, без устаревших v0.5/v0.6 исходного анализа.

## 15. Общая проверка готовности

Для изменения запускать тематические тесты, затем необходимые проверки проекта. Старое число тестов не доказывает текущее состояние.

- [ ] npm run check и npm test проходят на согласованной версии.
- [ ] Миграция проверена на копии существующей БД; backup/восстановление работают.
- [ ] ПК → телефон → ПК пройден с реальным Pi.
- [ ] Два клиента, потеря сети/ACK, retry и ответы на один запрос не ломают историю.
- [ ] Проверены backlog больше лимита страницы и очистка streaming-delta.
- [ ] Crash до/после передачи команды проверен fault injection; неопределённость не скрывается.
- [ ] STOP проверен при генерации, verification, ожидании ресурса, repair.
- [ ] Apply/Keep branch/cleanup сохраняют непроверенный или несохранённый результат до явного решения пользователя.
- [ ] Vercel проверен отдельно от mock/unit-тестов; непроверенные условия перечислены.
- [ ] Завершённый этап обновляет README, конфигурационные примеры и инструкции.

Первый крупный результат: одна управляемая Pi-сессия из терминала ПК и с телефона, с сохранением истории, ясными правилами команд и отключением клиента без остановки работы.
