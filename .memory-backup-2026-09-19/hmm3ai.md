# Проект: hmm3ai | Срез от 2026-09-13 21:12

# HMM3AI — R2B6: башня ПОДТВЕРЖДЕНА (+977 клеток), ошибка замера исправлена, поведение идентично ⇒ узкое место = политика

## Главный результат (лог `build/logs/r2b6_client.txt`, 53 дампа `build/logs/r2b6_observations/`)
- **Обзорная башня работает**: `knownTiles` (весь туман) 366 → **1313 (+947)** за один визит;
  `REVEAL_RESULT actionId=A1 hero=2277 target=(83 35 0) tilesRevealed=977 newObjectsDiscovered=101`.
  Сравнение: одна разведка даёт ~28 клеток / ~5 объектов. Башня — **≈34× по клеткам и ≈22× по объектам**
  за ~630-777 MP. Конфиг движка (`revealTiles radius 20` ≈1250 клеток) сходится ✓.
- **Ошибка была наша**: пакет награды приходит ПОСЛЕ первого чтения, поэтому «30 клеток». Фикс — мерить
  3 снимка и брать максимум; в логе видно прогрессию: `tilesRevealed=30 (still measuring)` →
  `977 (still measuring)` → `977`. Тот же фикс поднял и замер разведки: 17→32, 19→39, 13→37
  ⇒ опубликованные ранее «245 клеток за soak» были недосчитаны, честная средняя ≈28 клеток/разведку.
- За прогон измерено: 6 информационных действий = 5 разведок (142 клетки / 23 объекта) + 1 башня
  (977 / 101). Итого **1119 клеток / 124 объекта**.
- `revealsArea: true` теперь доезжает до дампов (49 записей `core:redwoodObservatory`) — баг сравнения со
  строкой `"REDWOOD_OBSERVATORY"` устранён, факт снимается в StateCapture.
- Башня после визита НЕ предлагается снова (player-global запись «уже пытались» её покрывает) ✓.

## РЕШАЮЩЕЕ наблюдение (ответ на «когда будет логика»)
Известная область выросла в ~3.5× (366 → 1558), а решения **идентичны** прогону R2B5:
38 действий, 23 552 MP, только `A1`×33 и `E1`×5, смена героя 10/37, **развороты >90° 15/36**.
То есть **знание ≠ логика**: восприятие больше не узкое место, узкое место — политика/непрерывность.
Поэтому дальше: R2C (память) → R2D (intent: доводить начатое) → R3 (сравнение политик + positional bias).

## Регрессии
R2B3 CHECKS PASS (11/11), R2B CHECKS PASS (17/17), R2A CHECKS PASS, R1B LIVE ACCEPTANCE PASS,
`test_bridge json` 0 ошибок, `explore` 14/14.

## Документы
`docs/R2B_acceptance.md`, `docs/R2B3_confirmation.md`, `docs/NK2_SOURCE_REUSE_PLAN.md`. Надо дописать в
R2B_acceptance: R2B.1 (башни: allow-list + `revealsArea` + честный вердикт + замер), исправленную
методику замера и скорректированные числа (245 → ≈28 клеток/разведку).
Окружение перезапущено (тег R2B7) — текущая сборка рабочая.